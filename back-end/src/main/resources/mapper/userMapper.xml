<?xml version="1.0" encoding="UTF-8" ?>

<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.example.backend.test.user.UserMapper">

    <select id="getUsers" parameterType="int" resultType="com.example.backend.test.user.User">
        SELECT * FROM user WHERE inspection_idx = #{inspection_idx};
    </select>
    <select id="loadUserByUserName" parameterType="map" resultType="com.example.backend.test.user.User">
        SELECT * FROM user WHERE user_id = #{user_id};
    </select>

    <insert id="insertUserInfo" parameterType="com.example.backend.test.result.UserResult" useGeneratedKeys="true">
        INSERT INTO user(inspection_idx, user_name, user_age, user_gender,cdate) VALUES (#{inspection_idx}, #{user_info.user_name}, #{user_info.user_age}, #{user_info.user_gender}, now());
        <selectKey keyProperty="user_idx" resultType="int" order="AFTER">
            select LAST_INSERT_ID();
        </selectKey>
    </insert>
    <insert id="insertUserAnswers" parameterType="int">
            INSERT INTO user_answer(user_idx, question_idx, answer_idx) VALUES (#{user_idx}, #{question_idx}, #{answer_idx} )
    </insert>



</mapper>
