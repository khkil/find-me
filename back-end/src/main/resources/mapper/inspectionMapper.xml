<?xml version="1.0" encoding="UTF-8" ?>

<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.example.backend.mapper.InspectionMapper">

    <select id="getInspectionList" resultType="com.example.backend.model.Inspection">
        SELECT * FROM inspection
    </select>

    <select id="getInspectionDetail" resultType="com.example.backend.model.Inspection">
        SELECT * FROM inspection WHERE idx = #{idx};
    </select>

</mapper>
